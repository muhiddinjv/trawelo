// avoid regeneratorTime error for tests
require('regenerator-runtime/runtime');
require('node-fetch');
// Add { "test": "jest --coverage", } to package.json and run `npm test` to generate a new folder called "coverage". Then, run "Open with Live Server" on the index.html file in "coverage > Icov-report" in VScode to see a full coverage of all the tests.